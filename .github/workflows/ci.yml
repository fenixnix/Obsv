name: CI Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  PROJECT_NAME: Obsv
  SOLUTION_FILE: Obsv.Avalonia.slnx
  MAIN_PROJECT: src/Obsv.Avalonia/Obsv.Avalonia.csproj

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.x'
        
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_FILE }}
      
    - name: Build
      run: dotnet build ${{ env.SOLUTION_FILE }} --configuration Release --no-restore
      
    - name: Test
      run: dotnet test ${{ env.SOLUTION_FILE }} --no-build --verbosity normal
      
    - name: Publish Windows x64
      run: |
        dotnet publish ${{ env.MAIN_PROJECT }} `
          -c Release `
          -r win-x64 `
          --self-contained true `
          -p:PublishSingleFile=true `
          -p:IncludeNativeLibrariesForSelfExtract=true `
          -o publish/win-x64
          
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: Obsv-Windows-x64
        path: publish/win-x64/**
        if-no-files-found: error

  build-linux:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.x'
        
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-0 libx11-xcb1 libxcb-dri3-0 libxcb-present0 libxcb-randr0 libxcb-xfixes0 libxcb-shape0 libxcb-shm0 libxext6 libxrender1 libxtst6 libxi6 libasound2t64
        
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_FILE }}
      
    - name: Build
      run: dotnet build ${{ env.SOLUTION_FILE }} --configuration Release --no-restore
      
    - name: Test
      run: dotnet test ${{ env.SOLUTION_FILE }} --no-build --verbosity normal
      
    - name: Publish Linux x64
      run: |
        dotnet publish ${{ env.MAIN_PROJECT }} \
          -c Release \
          -r linux-x64 \
          --self-contained true \
          -p:PublishSingleFile=true \
          -p:IncludeNativeLibrariesForSelfExtract=true \
          -o publish/linux-x64
          
    - name: Upload Linux artifact
      uses: actions/upload-artifact@v4
      with:
        name: Obsv-Linux-x64
        path: publish/linux-x64/**
        if-no-files-found: error

  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.x'
        
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'microsoft'
        java-version: '17'
        
    - name: Setup .NET Workloads
      run: |
        dotnet workload install android
        
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_FILE }}
      
    - name: Build Android
      run: dotnet build ${{ env.MAIN_PROJECT }} --configuration Release --no-restore --runtime android-arm64
      
    - name: Publish Android APK
      run: |
        echo "Publishing Android APK..."
        dotnet publish ${{ env.MAIN_PROJECT }} \
          -c Release \
          -r android-arm64 \
          --self-contained true \
          -p:AndroidPackageFormat=apk \
          -p:ApplicationId=com.fenixnix.obsv \
          -p:ApplicationVersion=1.0.0 \
          -p:ApplicationVersionCode=1 \
          -p:AndroidSdkDirectory=/usr/local/lib/android/sdk \
          -p:AndroidNdkDirectory=/usr/local/lib/android/sdk/ndk/25.1.8937393 \
          -p:JavaSdkDirectory=/opt/hostedtoolcache/Java_Microsoft_jdk/17.0.10/x64 \
          -o publish/android-apk
          
    - name: Find APK files
      run: |
        echo "Looking for APK files..."
        find . -name "*.apk" -type f || true
        find publish -name "*.apk" -type f || true
        find publish/android-apk -name "*.apk" -type f || true
        ls -la publish/android-apk/ || true
        ls -la publish/ || true
        
    - name: Upload Android APK
      uses: actions/upload-artifact@v4
      with:
        name: Obsv-Android-APK
        path: |
          **/*.apk
          publish/**/*.apk
          publish/android-apk/**/*.apk
        if-no-files-found: warn

  release:
    needs: [build-windows, build-linux, build-android]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: Obsv-Windows-x64
        path: artifacts/win-x64
        
    - name: Download Linux artifact
      uses: actions/download-artifact@v4
      with:
        name: Obsv-Linux-x64
        path: artifacts/linux-x64
        
    - name: Download Android artifact
      uses: actions/download-artifact@v4
      with:
        name: Obsv-Android-APK
        path: artifacts/android
        
    - name: Create Windows zip
      run: |
        cd artifacts/win-x64
        zip -r ../../Obsv-Windows-x64.zip .
        
    - name: Create Linux tar.gz
      run: |
        cd artifacts/linux-x64
        tar -czvf ../../Obsv-Linux-x64.tar.gz .
        
    - name: Create Release APK
      uses: softprops/action-gh-release@v1
      with:
        files: |
          Obsv-Windows-x64.zip
          Obsv-Linux-x64.tar.gz
          artifacts/android/**/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
